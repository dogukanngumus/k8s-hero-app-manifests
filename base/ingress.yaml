apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hero-app-ingress
spec:
  ingressClassName: nginx   
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hero-app-service
            port:
              number: 80